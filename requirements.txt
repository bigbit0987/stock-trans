# AlphaHunter - 尾盘低吸量化交易系统
# 依赖包列表

# 环境变量
python-dotenv>=1.0.0

# 数据获取
akshare>=1.12.0

# 数据处理
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0          # Parquet格式缓存

# HTTP 请求 (推送通知)
requests>=2.28.0

# 定时任务
schedule>=1.2.0

# 增强稳定性 (v2.4 新增)
tenacity>=8.2.0            # 指数退避重试
portalocker>=2.8.0         # 跨平台文件锁

# 可视化 (可选)
streamlit>=1.28.0
plotly>=5.15.0
